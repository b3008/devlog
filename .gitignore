__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
.coverage
htmlcov/
.pytest_cache/

# Runtime session-coverage log written by the devlog SessionEnd hook —
# per-machine data, not source. `devlog status` reads it locally.
.devlog/sessions.jsonl
